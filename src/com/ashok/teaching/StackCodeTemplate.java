package com.ashok.teaching;

/**
 * LinkedList can be used for stack implementation.
 * for each push and pop use the position (head or tail).
 *
 * @author Ashok Rajpurohit (ashok1113@gmail.com).
 */
public class StackCodeTemplate {
    final SinglyLinkedList list = new SinglyLinkedList();

    public void push(int value) {
        // head the value to your default position in list(head or tail).
    }

    public int pop(int value) {
        // remove the value from your default position in list.
        return 0; // dummy value.
    }

    public int size() {
        // return size from list.
        return 0; // dummy value.
    }
}
